{
  "name": "gift",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*",
    "configs/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "docker": "docker-compose -f ./configs/docker/docker-compose.development.yml up -d"
  },
  "devDependencies": {
    "husky": "^8.0.1"
  }
}
